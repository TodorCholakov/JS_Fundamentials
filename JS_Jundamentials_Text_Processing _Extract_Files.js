//OK 100/100
function solve (str){
    let arr = str.split("")
    for (let i=arr.length-1; i>0; i--){
        //console.log (arr[i])
        if (arr[i]=="."){
            str.indexOf(arr[i])
            let res1 = str.substring(38)
            console.log (res1)
            console.log (str.charCodeAt(34))
        } else if (arr[i]=="\"){
            str.indexOf(arr[i])
            let res2 = str.substring(38)
            console.log (res2)
        }

    }
}

solve ('C:\\Internal\\training-internal\\Template.pptx')