//OK 100/100
function nxxmatrix (param){
    let arr =[]
for (let a=0; a<param; a++) {
    arr=[];
    for (let b=0; b<param; b++){
        arr.push(param)
    }
    console.log (arr.join(' '))
    
}}
nxxmatrix (7)